# Rust
/target
**/*.rs.bk
apps/desktop/node_modules/
apps/desktop/dist/
apps/desktop/src-tauri/gen/

# Visual-companion brainstorm scratch (not part of the project)
.superpowers/

# Local databases / runtime artifacts
*.db
*.db-journal
*.db-wal
*.sock

# macOS
.DS_Store

# Secrets that must never land in the repo (APNs signing keys live beside the config)
*.p8
