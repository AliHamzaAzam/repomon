# Rust
/target
**/*.rs.bk
apps/desktop/node_modules/
apps/desktop/dist/
apps/desktop/src-tauri/gen/

# Visual-companion brainstorm scratch (not part of the project)
.superpowers/

# Local databases / runtime artifacts
*.db
*.db-journal
*.db-wal
*.sock

# macOS
.DS_Store

# Secrets that must never land in the repo (APNs signing keys live beside the config)
*.p8

# Coordinator process artifacts: briefs, audit reports, QA evidence stay local
/qa/
/.briefs/
docs/design-audit-*.md

# Design sources, plans and specs are not part of the shipped app or its documentation
/docs/brand/
/docs/superpowers/
/design/
/STATUS.md
/docs/windows-validation.md
